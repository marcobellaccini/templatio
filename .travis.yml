language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7-dev" # 3.7 development branch
  - "nightly" # currently points to 3.7-dev

# command to install program dependencies
install:
  - pip install textfsm Jinja2

# command to run tests
script: python -m unittest discover

